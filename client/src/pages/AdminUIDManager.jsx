import React, { useState } from 'react';
export const AdminUIDManager = () => { const [key, setKey] = useState(''); const [count, setCount] = useState(10); const [batch, setBatch] = useState(''); const [tokens, setTokens] = useState([]); const [message, setMessage] = useState(''); const headers = { 'Content-Type': 'application/json', 'x-admin-key': key }; const load = async () => { const response = await fetch('/api/admin/identities', { headers }); const data = await response.json(); setTokens(data.data || []); }; const generate = async () => { const response = await fetch('/api/admin/identities/generate', { method: 'POST', headers, body: JSON.stringify({ count, assignedBatch: batch }) }); const data = await response.json(); setMessage(data.message || `Generated ${data.data?.length || 0} UIDs`); load(); }; return <div className="mx-auto max-w-6xl px-4 py-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-brand-500">Administration</p><h1 className="mt-2 text-4xl font-display font-bold">UID ledger</h1></div><a href="/api/admin/identities/export" className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold dark:border-slate-700">Export CSV</a></div><section className="glass-panel-glow mt-8 rounded-3xl p-6"><div className="grid gap-3 md:grid-cols-4"><input type="password" value={key} onChange={(event) => setKey(event.target.value)} placeholder="Admin API key" className="rounded-xl border border-slate-200 bg-transparent px-3 py-3 dark:border-slate-700" /><input type="number" min="1" max="5000" value={count} onChange={(event) => setCount(Number(event.target.value))} className="rounded-xl border border-slate-200 bg-transparent px-3 py-3 dark:border-slate-700" /><input value={batch} onChange={(event) => setBatch(event.target.value)} placeholder="Batch name" className="rounded-xl border border-slate-200 bg-transparent px-3 py-3 dark:border-slate-700" /><div className="flex gap-2"><button onClick={generate} className="rounded-xl bg-brand-500 px-4 py-3 font-bold text-white">Generate</button><button onClick={load} className="rounded-xl border border-slate-200 px-4 py-3 font-bold dark:border-slate-700">Refresh</button></div></div>{message && <p className="mt-4 text-sm text-brand-500">{message}</p>}</section><div className="mt-6 overflow-x-auto rounded-2xl border border-slate-200 dark:border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-100 dark:bg-slate-900"><tr><th className="p-3">UID</th><th className="p-3">Status</th><th className="p-3">Batch</th><th className="p-3">Expires</th></tr></thead><tbody>{tokens.map((token) => <tr key={token.uid} className="border-t border-slate-200 dark:border-slate-800"><td className="p-3 font-mono">{token.uid}</td><td className="p-3">{token.status}</td><td className="p-3">{token.assignedBatch}</td><td className="p-3">{new Date(token.expiresAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></div>; };