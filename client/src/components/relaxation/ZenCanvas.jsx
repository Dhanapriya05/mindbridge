import React, { useEffect, useRef } from 'react';
export const ZenCanvas = () => { const canvasRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; const context = canvas.getContext('2d'); const particles = []; let frame; const resize = () => { canvas.width = canvas.clientWidth * devicePixelRatio; canvas.height = canvas.clientHeight * devicePixelRatio; context.scale(devicePixelRatio, devicePixelRatio); }; const move = (event) => { const rect = canvas.getBoundingClientRect(); for (let index = 0; index < 8; index += 1) particles.push({ x: event.clientX - rect.left, y: event.clientY - rect.top, life: 1, size: 2 + Math.random() * 5, dx: (Math.random() - 0.5) * 1.5, dy: (Math.random() - 0.5) * 1.5 }); }; const draw = () => { context.fillStyle = 'rgba(11, 15, 25, .16)'; context.fillRect(0, 0, canvas.clientWidth, canvas.clientHeight); particles.forEach((particle) => { particle.x += particle.dx; particle.y += particle.dy; particle.life -= 0.008; context.fillStyle = `rgba(56, 189, 248, ${particle.life})`; context.beginPath(); context.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2); context.fill(); }); while (particles.length && particles[0].life <= 0) particles.shift(); frame = requestAnimationFrame(draw); }; resize(); window.addEventListener('resize', resize); canvas.addEventListener('pointermove', move); draw(); return () => { cancelAnimationFrame(frame); window.removeEventListener('resize', resize); canvas.removeEventListener('pointermove', move); }; }, []); return <section className="glass-panel-glow rounded-3xl p-6"><h3 className="text-2xl font-display font-bold">Zen Drift</h3><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">Move slowly across the field and let the motion dissolve.</p><canvas ref={canvasRef} className="mt-5 h-64 w-full rounded-2xl bg-[#0B0F19] touch-none" /></section>; };