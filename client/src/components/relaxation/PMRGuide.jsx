import React, { useEffect, useState } from 'react';
const GROUPS = ['Feet', 'Hands', 'Shoulders', 'Jaw'];
export const PMRGuide = () => { const [active, setActive] = useState(false); const [index, setIndex] = useState(0); const [seconds, setSeconds] = useState(5); const [tension, setTension] = useState(true); useEffect(() => { if (!active) return undefined; const timer = setInterval(() => setSeconds((value) => { if (value > 1) return value - 1; setTension((old) => !old); if (!tension) setIndex((old) => (old + 1) % GROUPS.length); return 5; }), 1000); return () => clearInterval(timer); }, [active, tension]); return <section className="glass-panel-glow rounded-3xl p-6"><h3 className="text-2xl font-display font-bold">Progressive Muscle Relaxation</h3><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">Gently {tension ? 'tense' : 'release'} your {GROUPS[index].toLowerCase()} for {seconds} seconds.</p><div className="my-8 grid grid-cols-4 gap-2">{GROUPS.map((group, item) => <div key={group} className={`rounded-xl p-3 text-center text-xs ${item === index ? 'bg-calm-cyan/20 text-calm-cyan' : 'bg-slate-100 dark:bg-slate-800'}`}>{group}</div>)}</div><button onClick={() => setActive((value) => !value)} className="rounded-xl bg-brand-500 px-6 py-3 text-white font-bold">{active ? 'Pause scan' : 'Begin scan'}</button></section>; };